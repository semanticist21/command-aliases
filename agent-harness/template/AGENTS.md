# <project>

<one line: what this repo is.>

Follows the global harness (`~/.claude/CLAUDE.md`). Where they differ, this file wins.

## Mechanics

- Run `<cmd>` · Test `<cmd>` · Build `<cmd>`

## Decisions

Only what the user said. The global harness owns the format.
