interface:
  display_name: "Task"
  short_description: "Prepare a worktree, then plan, execute, and QA"
  default_prompt: "Use $task to create a prepared isolated worktree, then plan, execute, and verify this task until findings are zero."

policy:
  allow_implicit_invocation: true
