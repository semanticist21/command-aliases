interface:
  display_name: "Task Run SSH"
  short_description: "Run isolated heavy jobs over SSH worktrees"
  default_prompt: "Use $task-run-ssh to run this heavy job on the recorded SSH target without guessing the remote username."
