interface:
  display_name: "Memo"
  short_description: "Save durable notes from memo arguments"
  default_prompt: "Use $memo to save this instruction, preference, follow-up, or environment fact as a durable note."

policy:
  allow_implicit_invocation: true
