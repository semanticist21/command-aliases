interface:
  display_name: "Memo"
  short_description: "Route and save a one- or two-line durable note"
  default_prompt: "Use $memo to save this as a minimal durable project or user note."

policy:
  allow_implicit_invocation: false
