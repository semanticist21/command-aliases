interface:
  display_name: "Secrets Sync"
  short_description: "Bootstrap and synchronize agent access and secrets"
  default_prompt: "Use $secrets-sync user to bootstrap this machine and synchronize my registered credentials and project inputs."

policy:
  allow_implicit_invocation: false
