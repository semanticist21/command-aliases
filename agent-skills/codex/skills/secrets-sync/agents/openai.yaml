interface:
  display_name: "Secrets Sync"
  short_description: "Synchronize ignored project secrets with NAS"
  default_prompt: "Use $secrets-sync user to restore my registered shared credentials, or $secrets-sync for this project's ignored inputs."

policy:
  allow_implicit_invocation: false
