interface:
  display_name: "Task Runner Setup"
  short_description: "Configure repo-aware remote heavy-job runners"
  default_prompt: "Use $task-runner-setup to reuse the existing OrbStack runner for this repository without asking me to choose a runner backend."
