interface:
  display_name: "Natural Writing"
  short_description: "Natural Korean and English rewrites"
  default_prompt: "Use $natural-writing to rewrite this text naturally while preserving meaning, facts, names, and constraints."

policy:
  allow_implicit_invocation: true
